# Flask secret key (required - change this in production, min 32 bytes)
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Flask debug mode
DEBUG=True

# API host/port
API_HOST=0.0.0.0
API_PORT=5000
